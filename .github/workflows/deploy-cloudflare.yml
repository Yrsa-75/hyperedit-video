# Workflow désactivé — le déploiement est géré par Cloudflare Pages directement.
name: Deploy (disabled)
on:
  workflow_dispatch:
  jobs:
    deploy:
        runs-on: ubuntu-latest
            if: false
                steps:
                      - run: echo "disabled"
